# Railway: slim API image (Dockerfile.railway). In the dashboard add GROQ_API_KEY and/or OPENAI_API_KEY.
# https://docs.railway.com/reference/config-as-code

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.railway"
